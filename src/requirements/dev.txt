# Inherits everything from base
-r base.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7     # async test support
pytest-cov==5.0.0          # coverage reports
respx==0.21.1              # mock httpx requests in tests

# ── Code quality ──────────────────────────────────────────────────────────────
ruff==0.4.7                # linter + formatter (replaces flake8 + black)
mypy==1.10.0               # static type checking

# ── Dev utilities ─────────────────────────────────────────────────────────────
ipython==8.25.0            # better REPL for manual testing